@model IEnumerable<NewwProject.Models.CategoryFp>

@{
    Layout = "~/Views/_admin.cshtml";
}
<style>
    .footer {
   position: fixed;
   bottom: 10px;
   text-align: center;
}
</style>

<br /><br />
<div class="row column1">

    @foreach (var item in Model)
    {


         <div class="col-md-6 col-lg-4">
            <div class="full white_shd margin_bottom_30">
               <div class="info_people" style="min-height:200px;max-height:200px">
                  <div class="p_info_img">
                     <img src=@Url.Content("~/Images/"+item.CategoryImage) asp-append-version="true" />
                  </div>
                  <div class="user_info_cont">
                     <h4>@item.CategoryName</h4>
                     
                     
                  </div>
                           <a asp-action="Edit" class="btn btn-success" style="height:37px;margin-left:5px" asp-route-id="@item.CategoryId">Edit</a> 
                       <br>
@*                             <a asp-action="Details" class="btn btn-success" style="height:37px;margin-left:5px"  asp-route-id="@item.CategoryId">Details</a> 
*@                     <br>
                          <a asp-action="Delete" class="btn btn-success" style="height:37px;margin-left:5px"  asp-route-id="@item.CategoryId">Delete</a>
               </div>

            </div>
         </div>
    }
</div>
<a class="btn btn-success" asp-action="Create">Create New</a>




















@* <div class="container-fluid">
                  
                   

@foreach (var item in Model) {
           <div class="row column_title">
                            <div class="col-md-12">
                               <div class="page_title">
                                
<div class="card" style="width: 18rem;">
  <img class="card-img-top" src=@Url.Content("~/Images/"+item.CategoryImage) asp-append-version ="true" alt="Card image cap">
  <div class="card-body">
    <h5 class="card-title">@item.CategoryName</h5>
    <p class="card-text">Some quick example text to build on the card title and make up the bulk of the card's content.</p>
        <a asp-action="Edit" class="btn btn-success" asp-route-id="@item.CategoryId">Edit</a> 
             <a asp-action="Details" class="btn btn-success" asp-route-id="@item.CategoryId">Details</a> 
              <a asp-action="Delete" class="btn btn-success" asp-route-id="@item.CategoryId">Delete</a>
  </div>
</div>
</div>
 </div>
 </div>
}
        </div>
                     
<p>*@
@*    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
               Category Name
            </th>
            <th>
               Category Image)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CategoryName)
            </td>
            <td>
                <img src=@Url.Content("~/Images/" + item.CategoryImage) asp-append-version ="true" style="border-radius:20%;" width="60" height="60" />

            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CategoryId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CategoryId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CategoryId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
*@